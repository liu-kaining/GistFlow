1. **初始化项目：**
> "I am building GistFlow. Here is the **Project Charter and Phase 2 Tech Stack**. Please initialize the project folder structure, `pyproject.toml` with the specified libraries, and the `.env` configuration loader." (附上 Phase 1 & 2 文档)


2. **构建核心逻辑 (Ingestion & Cleaning)：**
> "Now implement the **Ingestion and Cleaner modules** based on the **Phase 5 Component Specifications**. I need `EmailFetcher` and `ContentCleaner` classes. Ensure you handle HTML-to-Markdown conversion robustly." (附上 Phase 5 的 5.2 和 5.3)


3. **构建 AI 大脑 (LLM)：**
> "Implement the **LLM Engine**. Here is the **Phase 3 Data Contract (Pydantic Model)** and the **Prompt Design**. Use LangChain's structured output to ensure the AI strictly follows the `Gist` schema." (附上 Phase 3 文档)


4. **构建输出层 (Notion)：**
> "Implement the **Notion Publisher**. Map the `Gist` object to Notion properties as defined in the **Phase 3 Schema**. Ensure handling of long text in blocks." (附上 Phase 3 的 3.2 和 Phase 5 的 5.5)


5. **组装与运行 (Main Loop)：**
> "Finally, tie everything together in `main.py` using `APScheduler`. It should fetch -> clean -> analyze -> push -> archive. Add graceful error handling so one bad email doesn't crash the loop."


6. **Docker 化：**
> "Generate the `Dockerfile` and `docker-compose.yml` based on the **Phase 6 Deployment Docs**."